#include "stdafx.h"
/*
static OusnsUser ^ find_user_by_id(OusnsLoginUser ^ loginUser, int userID){
	for each(OusnsUser ^ f in loginUser->friendList) {
		if (f->userID == userID) {
			return f;
		}
	}
}
*/